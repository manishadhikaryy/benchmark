#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

int main() {
    // Initialize time
    clock_t initial_time = clock();

    // set up registers
    register double num;
    register double num1 = 1.1;
    register double num2 = 1.2;

    double time_taken = 0.0;
    return 0; 
}